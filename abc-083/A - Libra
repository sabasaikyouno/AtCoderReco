import java.util.Scanner

object Main extends App {
  val sc = new Scanner(System.in)
  val a, b, c, d = sc.nextInt()
  val ab = a + b
  val cd = c + d
  if(ab > cd) {
    println("Left")
  } else if (ab == cd) {
    println("Balanced")
  } else {
    println("Right")
  }
}
